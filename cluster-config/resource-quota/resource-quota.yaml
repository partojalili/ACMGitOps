apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-quota
  namespace: gitops-demo-quotas
  labels:
    managed-by: argocd
spec:
  hard:
    requests.cpu: "4"
    requests.memory: "4Gi"
    limits.cpu: "8"
    limits.memory: "8Gi"
    pods: "20"
    services: "10"
