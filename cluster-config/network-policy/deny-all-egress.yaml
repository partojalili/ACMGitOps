apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-egress
  namespace: gitops-demo-secured
  labels:
    managed-by: argocd
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress: []
